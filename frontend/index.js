import React from "react";
import ReactDOM from "react-dom/client"; // Ensure you are using 'react-dom/client'
import App from "./App";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
